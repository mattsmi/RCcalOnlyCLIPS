#!/bin/bash

#Runs the test CLIPS expert system, and leaves you in the CLIPS interpreter
#   for further analysis. Exit with: "(exit)".

#Using the -f option will echo the commands to the command prompt. 
#   Using the -f2 option will execute the commands without echoing 
#   the commands to the command prompt.
/opt/clips630/clips630 -f2 RomanTester.clp

